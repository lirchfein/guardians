<div class="container">
  <div class="row">
    <% @guards.each do |g| %>
    <div class="col-xs-12 col-sm-6 col-md-4">
      <% @guard = g %>
      <%= link_to (render 'components/card', guard: g), guard_path(g.id) %>
    </div>
    <% end %>


  </div>
</div>


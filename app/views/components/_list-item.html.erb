<% if booking.completed %>
  <div class="list-item completed">
<% else %>
  <div class="list-item">
<% end %>
    <%= cl_image_tag(booking.guard.picture, :width=>100, :height=>100, :crop=>"fill") %>
    <div class="list-content">
      <%= link_to guard_path(booking.guard), class: 'list-link' do %>
        <h3><%= booking.guard.name %></h3>
      <% end %>
      <% if booking.completed %>
        <p>MISSION ACCOMPLISHED</p>
      <% elsif !booking.confirmed %>
        <p>PENDING MISSION REQUEST</p>
      <% else %>
        <p>ON YOUR MISSION</p>
      <% end %>
    </div>
    <div>
      <div>
        <% if !booking.confirmed %>
          <%= form_for :booking, url: cancel_booking_path(booking), method: :patch do |f| %>
              <%= f.submit 'Cancel', class: 'btn-plain no-padding cancel-btn' %>
          <% end %>
        <% else %>
          <%= form_for :booking, url: booking_path(booking), method: :patch do |f| %>
              <%= f.submit 'Mark Mission as completed', class: 'btn-plain no-padding complete-btn' %>
          <% end %>
        <% end %>
      </div>

      <%= cl_image_tag "tick-full", id:'tick-full'%>
    </div>
  </div>
